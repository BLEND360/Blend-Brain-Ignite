VITE_API_BASE_URL=/api/v1
# Local development only; omit when the API is served from the same origin.
VITE_API_PROXY_TARGET=http://localhost:8000
# Local laptop authentication only. Use an interactive OIDC session outside local development.
VITE_API_BEARER_TOKEN=replace-with-the-local-api-token
